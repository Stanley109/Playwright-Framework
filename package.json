{
  "dependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1"
  },
  "scripts": {
    "sit-headless": "npx cross-env TEST_ENV=sit playwright test --project=chromium",
    "sit-headed": "npx cross-env TEST_ENV=sit playwright test --project=chromium --headed",
    "uat-headless": "npx cross-env TEST_ENV=uat playwright test --project=chromium",
    "uat-headed": "npx cross-env TEST_ENV=uat playwright test --project=chromium --headed"
  }
}
